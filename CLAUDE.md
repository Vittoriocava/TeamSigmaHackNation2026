# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

TeamSigmaHackNation2026 — a Python project currently in initial setup. The `.gitignore` is configured for Python development with Ruff (linting/formatting), pytest (testing), and common dependency managers (Poetry, PDM, pip).

## Commands

No build system has been configured yet. As the project grows, update this section with the actual commands (e.g., `ruff check .`, `pytest`, `python -m <module>`).
